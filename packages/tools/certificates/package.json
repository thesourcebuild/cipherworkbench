{
  "name": "@ocs/certificates",
  "version": "0.11.3",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "//": "Certificates and PKI family: X.509 TLS/SSL certificates, PKCS#10 Certificate Signing Requests, and Certificate Format Converter. Pure client-side ESM implementation using standard WebCrypto and zero native dependencies. `./manifests` is eager and dependency-free; `./definition` is the on-demand half that loads the parsing and compute logic.",
  "exports": {
    ".": "./src/index.ts",
    "./pure": "./src/pure.ts",
    "./manifests": "./src/manifest.ts",
    "./definition": "./src/definition.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^2.1.0",
    "@noble/hashes": "^2.2.0",
    "@ocs/contracts": "workspace:*",
    "@ocs/engine": "workspace:*",
    "@scure/base": "^1.2.6",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@ocs/config": "workspace:*",
    "typescript": "^5.9.3"
  }
}
